services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
<<<<<<< HEAD
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - JWT_SECRET=${JWT_SECRET}

  frontend:
    build: ./mindbridge

  nginx:
    image: nginx:alpine
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "3000:3000"
    depends_on:
      - backend
      - frontend
=======
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
      - JWT_SECRET=local-dev-secret-key-for-testing-123456789

  frontend:
    build:
      context: ./mindbridge
      args:
        API_PROXY_TARGET: http://backend:8080
    ports:
      - "3000:3000"
    environment:
      - API_PROXY_TARGET=http://backend:8080
    depends_on:
      - backend
>>>>>>> 2bb2ef62b9902fd4c36412ff39432e6f45bb2bf3
